This code used to be in the Jupyter notebook but I shifted it over into regular python files. In doing so, I changed around the 
directories significantly, so much of the pathnames to images are broken. 
